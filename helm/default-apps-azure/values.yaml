name: default-apps-azure
serviceType: managed

project:
  branch: "[[ .Branch ]]"
  commit: "[[ .SHA ]]"

image:
  registry: quay.io
  name: giantswarm/default-apps-azure
  tag: v0.0.1
  pullPolicy: IfNotPresent
